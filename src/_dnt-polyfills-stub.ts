// Empty stub aliased in for `_dnt.polyfills.js` imports from npm-built
// `@bandeira-tech/b3nd-move`. The polyfill file exists for Node CJS
// (`import.meta` ponyfill); browsers never need it and Vite cannot
// bundle its `node:module`/`node:url`/`node:path` imports.
export {};
